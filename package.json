{
  "name": "clawora-commerce",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply DB --local",
    "db:remote": "wrangler d1 migrations apply DB --remote",
    "check": "node --check worker/index.js && node --check public/app.js"
  },
  "devDependencies": {
    "wrangler": "^4.37.0"
  }
}
